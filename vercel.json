{
  "rewrites": [
    { "source": "/", "destination": "/api" },
    { "source": "/(.*)", "destination": "/api" },
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "installCommand": "pnpm i --no-audit --no-fund --ignore-scripts"
}
